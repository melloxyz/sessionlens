{
  "name": "sessionlens",
  "private": true,
  "version": "0.9.6",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "changelog": "npx --yes git-cliff@latest --config .github/workflows/cliff.toml -o CHANGELOG.md --tag-pattern 'v[0-9]*'"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/sql.js": "^1.4.11",
    "eslint": "^9.14.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.2.0",
    "prettier": "^3.4.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.16.0"
  }
}
